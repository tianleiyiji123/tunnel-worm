# 2026-03-30 工作日志

## Logo 替换
- 采用方案 C（极简圆虫 · 橙红渐变）替换应用内所有 Logo
- 更新了 favicon.svg、AppHeader.vue、HomeView.vue 的 SVG 图标
- Logo 色系：橙红渐变(#f97316 → #ef4444)，白底(#fefefe)

## 用户登录系统（可选登录）
- 后端新增：
  - `database.py`: User、TransferRecord 模型；Transfer 加 user_id、permanent 字段
  - `config.py`: JWT_SECRET、JWT_ALGORITHM、JWT_EXPIRE_HOURS 配置
  - `deps.py`: 可选认证依赖注入 get_current_user
  - `services/auth_service.py`: 密码哈希、JWT 生成、用户 CRUD
  - `routes/auth.py`: 注册、登录、获取用户信息 API
  - `routes/records.py`: 操作记录分页查询 API
  - `schemas.py`: 新增 User/Record 相关 Schema
- 传输逻辑改造：
  - 登录用户的文本传输自动标记 permanent=True，永不过期
  - cleanup 跳过 permanent 记录
  - 每次 send/retrieve 都记录到 TransferRecord 表
- 依赖新增：python-jose[cryptography]、passlib[bcrypt]、sqlalchemy

## 前端用户系统
- `api/index.ts`: 新增 auth/records API + axios token 拦截器
- `composables/useAuth.ts`: 认证状态管理（login/logout/register/checkAuth）
- `components/LoginDialog.vue`: 登录/注册弹窗（ElDialog + Tabs）
- `components/AppHeader.vue`: 添加登录按钮 + 用户下拉菜单（我的记录/退出）
- `views/RecordsView.vue`: 操作记录页（发送/提取 Tab、分页、时间格式化）
- `router/index.ts`: 新增 /records 路由
- `main.ts`: 启动时 checkAuth

## README 重写
- 重写为专业开源文档风格：Feature badges、架构图、部署指南、配置表、安全说明

## Docker 部署
- 构建镜像: `docker compose build --no-cache`
- 首次构建后需要手动迁移旧数据库（添加 transfer.user_id 和 transfer.permanent 列）
- 启动命令: `docker compose up -d`，端口 7895
- 健康检查: GET /api/health → {"status":"ok","service":"suisuichong","initialized":true}
- 认证接口: GET /api/auth/me → {"user":null}（未登录时）

## Bug 修复
- **弹窗被裁剪**: LoginDialog 从 AppHeader(fixed) 移到 App.vue 顶层，通过 provide/inject 全局控制
- **bcrypt 72 字节限制**: auth_service.py 新增 SHA-256 预哈希 _prehash_password()，hash/verify 都先预哈希
- **passlib + bcrypt 版本不兼容**: passlib 1.7.4 不兼容 bcrypt 5.x（缺少 __about__ 模块），在 requirements.txt 和 requirements-docker.txt 中固定 bcrypt==4.2.1
- **Logo SVG 比例**: viewBox 从 0 0 200 200 改为 22 62 137 82，虫子可见占比从 ~30% 提升到 ~90%
- Logo viewBox 统一: `22 62 137 82` (width=137, height=82, ratio ≈ 1.67:1)
